# runic-test-support/src

Shared test-support modules.

## Modules

- `distributions`: deterministic workload distributions for allocation traces.
- `subprocess`: helpers for running aborting cases outside the harness process.
- `trace`: trace actions and execution machinery for allocator tests.

Keep this directory focused on test mechanics, not allocator implementation.
