# runic/src/bin

Helper binaries for integration tests.

`abort_case` runs one invalid allocator operation per process so abort behavior can be tested without killing the test harness.

These binaries are support tools, not user-facing CLI programs.
