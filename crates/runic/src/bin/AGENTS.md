# AGENTS.md

Scope: `crates/runic/src/bin/`.

- Binaries here support tests only.
- Keep abort cases single-purpose and parameterized by simple command-line arguments.
- Do not add user-facing CLI behavior without updating package metadata and docs.
